import { PaletteOptions } from "@mui/material";
import { colors } from "./colors";

export const dark: PaletteOptions = {
  primary: {
    main: colors.white
  },
  // secondary: {
  //   main: 'transparent'
  // }
}