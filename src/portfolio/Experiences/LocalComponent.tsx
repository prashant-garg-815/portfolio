import { styled } from '@mui/material/styles';
import { Paper } from '@mui/material';

export const StyledExperiences = styled(Paper, {
  name: 'Experiences'
})(() =>({
  backgroundColor: 'transparent',
}));